{
  "name": "roq/cme/filter/flags/Flags",
  "type": "flags",
  "values": [
    {
      "name": "type",
      "type": "std/string",
      "required": true,
      "description": "Type of output, e.g. :code:`tcpdump`."
    },
    {
      "name": "channel_ids",
      "type": "std/uint16",
      "array": "std/vector",
      "required": true,
      "description": "Comma-separated list of multicast channels to include."
    }
  ]
}
