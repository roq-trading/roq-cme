{
  "name": "Multicast",
  "type": "flags",
  "values": [
    {
      "name": "multicast_config_file",
      "type": "std::string",
      "validator": "roq::core::flags::Path<std::string>",
      "description": "config file (path)"
    },
    {
      "name": "multicast_channel_ids",
      "type": "std::vector<std::string>",
      "description": "Comma-seprated list of multicast channels, e.g. 310"
    },
    {
      "name": "multicast_local_interface",
      "type": "std::string",
      "description": "Multicast local interface"
    },
    {
      "name": "multicast_timeout",
      "type": "std::chrono::nanoseconds",
      "validator": "roq::core::flags::TimePeriod",
      "default": "90s",
      "description": "Timeout when waiting for snapshot"
    },
    {
      "name": "multicast_buffer_size",
      "type": "uint32_t",
      "default": "2048",
      "description": "Buffer size (at least the maximum possible size of a datagram)"
    },
    {
      "name": "multicast_buffer_depth",
      "type": "uint32_t",
      "default": "8",
      "description": "Multicast buffer depth (used for re-ordering)"
    }
  ]
}
